let circleArea = function (pi, r) {
    return pi * r * r;
};

let circleArea = (pi, r) => {
    return pi * r * r;
};

let circleArea = (pi, r) => pi * r * r;

circleArea(3.14, 3); // Always returns 28.26